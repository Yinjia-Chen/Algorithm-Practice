
# ByteIntern 前端算法面试 48 小时突击生存指南

> 适用对象：算法基础较弱、即将参加字节跳动 ByteIntern 前端面试的同学  
> 目标：不被算法直接刷掉，稳定通过算法关

---

## 一、核心结论（先定心）

- ByteIntern 前端 **不是算法岗**
- 面试官关注的是：**思路是否清楚 + 表达是否稳定 + 是否有培养潜力**
- 即使 LeetCode Medium 做不熟，只要：
  - 能识别题型
  - 能给出可行思路
  - 代码结构不崩
  - 能解释复杂度  
  👉 **完全有机会通过**

---

## 二、最低过线标准（务必达到）

你至少要做到：

1. 能判断题目属于哪一类（数组 / 字符串 / 树 / 工程题）  
2. 能先说思路，再写代码  
3. 能处理基础边界情况  
4. 能说清时间 / 空间复杂度  

> 面试不是 AC 竞赛，而是工程能力评估

---

## 三、必须掌握的 5 个救命算法模型

### 模型 1：双指针 / 滑动窗口（覆盖率最高）

```js
let left = 0;
const map = new Map();

for (let right = 0; right < arr.length; right++) {
  map.set(arr[right], (map.get(arr[right]) || 0) + 1);

  while (/* 不满足条件 */) {
    map.set(arr[left], map.get(arr[left]) - 1);
    left++;
  }

  // 更新答案
}
```

**面试话术**：  
> 使用滑动窗口，通过左右指针动态维护区间，整体时间复杂度是 O(n)。

---

### 模型 2：Map / Set 计数

```js
const map = new Map();
for (const x of arr) {
  map.set(x, (map.get(x) || 0) + 1);
}
```

适用：两数之和、去重、频率统计等

---

### 模型 3：树的 DFS（前端常考）

```js
function dfs(node) {
  if (!node) return 0;
  const left = dfs(node.left);
  const right = dfs(node.right);
  return Math.max(left, right) + 1;
}
```

适用：最大深度、翻转二叉树、路径问题

---

### 模型 4：一维 DP（只需会最基础）

```js
dp[0] = ...
dp[1] = ...
for (let i = 2; i < n; i++) {
  dp[i] = dp[i - 1] + dp[i - 2];
}
```

核心是：**说清 dp[i] 表示什么**

---

### 模型 5：前端工程保命题（强烈建议）

#### debounce

```js
function debounce(fn, delay) {
  let timer = null;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}
```

throttle、deepClone、数组转树 / 树转数组、扁平化嵌套结构

工程题即使没写完，也非常加分

---

## 四、48 小时极限复习安排

### Day 1
- 上午：背滑动窗口 + Map 模板
- 下午：树 DFS 模板 + 手写 2 次
- 晚上：debounce / deepClone（能讲清即可）

### Day 2（面试前一天）
- 上午：模拟讲 2 道题（必须开口说）
- 下午：复习边界条件
- 面试前：只看模板，不刷新题

---

## 五、面试保命话术

- 卡住时：  
  > 我先给出一个直观解法，再考虑优化。

- 没写完：  
  > 核心逻辑已完成，剩下是边界和工程细节。

- 时间不够：  
  > 如果时间允许，可以进一步优化空间复杂度。

---

## 六、最后一句话

> **稳住思路 + 清楚表达 > 刷题数量**  
> ByteIntern 看的是潜力，而不是熟练度。
