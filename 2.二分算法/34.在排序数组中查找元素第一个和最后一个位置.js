/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */

// 二分查找解法：先封装一个二分查找的方法
let binarySearch = (nums,target) => {
  
}

var searchRange = function(nums, target) {
  let res = [-1, -1]; // 未找到不更新res，直接返回[-1,-1]
  return res;
};



// 输入：nums = [5,7,7,8,8,10,12], target = 8
// 输出：[3,4]